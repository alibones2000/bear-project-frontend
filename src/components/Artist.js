import React from "react";

function Artist({ srtist }) {

    //fill in with keys from artist table
    const { } = skatespot

    return (
        <div>

        </div>
    )



}

export default Skatespot;